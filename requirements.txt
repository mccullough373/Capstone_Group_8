PyQt6>=6.6
PyQt6-Qt6>=6.6
reportlab>=4.0
Pillow>=10.0
# Optional (choose one if you plan to run a real model locally):
# tensorflow>=2.15 ; platform_system != "Darwin"  # wheels availability may vary for Python 3.14
# tflite-runtime  # alternative minimal runtime; install via platform-specific wheels
# onnxruntime
